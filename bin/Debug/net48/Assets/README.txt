Put the ChewyPetsFeed logo here as:  logo.png

- Any size works; a square image around 256-512px looks best in the top bar
  and on the login screen.
- If this file is missing the app still runs and just shows the "ChewyPetsFeed"
  wordmark with no icon.

The build copies Assets\ next to StockDesk.exe automatically (see StockDesk.vbproj).
